# Product Factory frontend

Nieuwe Flutter-webfrontend voor Product Factory. De eerste versie toont alleen de herkenbare
applicatieschil en een lege productpagina.

```bash
flutter pub get
flutter analyze
flutter test
flutter build web --release --pwa-strategy=none
```
