# Stappenplan gedeelde Agent Runtime — verhuisd

Dit stappenplan is verhuisd naar de eigen repository van de Agent Runtime:

`/Users/robbertvdzon/git/agent-runtime/docs/agent-runtime-stappenplan.md`

Die repository is nu de enige bron voor deze roadmap. Dit bestand blijft alleen als verwijzing staan voor wie via deze repo op het oude pad terechtkomt.
