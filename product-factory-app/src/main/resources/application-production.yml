spring:
  datasource:
    url: ${PF_DB_URL}
    username: ${PF_DB_USERNAME}
    password: ${PF_DB_PASSWORD}
    hikari:
      data-source-properties:
        connectTimeout: 5
        socketTimeout: 30
        options: -c statement_timeout=30000
