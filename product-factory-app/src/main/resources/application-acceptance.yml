spring:
  datasource:
    url: jdbc:h2:mem:product-factory-acceptance;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  flyway:
    locations: classpath:db/migration

PF_SCHEDULES_ENABLED: false
PF_AI_PROVIDER: MOCKED
PF_SOFTWARE_FACTORY_MODE: MOCKED
PF_EXTERNAL_MUTATIONS_ALLOWED: false
